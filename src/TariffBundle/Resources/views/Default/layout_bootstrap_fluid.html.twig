<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{% block title %}{% endblock %} — {{ webapp_name }}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="author" content="Okto">

        <!-- Le styles -->
        {% block stylesheets %}
            <link href="{{ asset('bundles/tariff/external/bootstrap/bootstrap-cosmo.min.css') }}" rel="stylesheet" />
            <style>
                footer{text-align: center}
            </style>
            <link href="{{ asset('bundles/tariff/external/bootstrap/bootstrap-responsive.css') }}" rel="stylesheet" />
            <link href="{{ asset('bundles/tariff/external/fontello/css/font-awesome-custom.css') }}" rel="stylesheet" />
        {% endblock %}
        <!-- Unoverrridable -->
        <link href="{{ asset('bundles/tariff/css/styles.css') }}" rel="stylesheet" />


        <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
        <!--[if lt IE 9]>
          <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->

        <!-- Fav and touch icons -->
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>

    <body>
        <div class="page-container">
            <!--Навигация в шапке-->
        {% block header_nav %}{% endblock %}
        <!--/Навигация в шапке-->

        <div class="container-fluid">
            <div class="row-fluid">
                {#<div class="span1">

                    <!--Навигация-->
                {% block sidebar_nav %}{% endblock %}
                <!--/Навигация-->

            </div><!--/span-->#}

            <div class="span12">
                <h2 class="page-header">{% block pagetitle %}{% endblock %}</h2>
                <!-- Хлебные крошки -->
            {% block breadcrumbs %}{% endblock %}
            <!-- /Хлебные крошки -->

            <!-- Сообщения -->
            {% if app.session.flashBag|length %}

                {% for key in app.session.flashBag.keys() %}
                    {% for msg in app.session.flashBag.get(key) %}
                        <div class="alert alert-dismissible alert-{{ key == 'error'? 'danger' : key }}">
                            <button type="button" class="close" data-dismiss="alert">&times;</button>

                            {{ msg }}
                        </div>
                    {% endfor %}
                {% endfor %}

            {% endif %}
            <!-- /Сообщения -->

        {% block body %}{% endblock %}

    </div><!--/span-->

    {#<div class="span1">

    </div><!--/span-->#}

</div><!--/row-->
</div><!--/.fluid-container-->

<hr>

<footer class="navbar-fixed-bottom">
    <p>&copy; 2016 {{ webapp_name }}</p>
</footer>

</div>
{% block javascripts %}
    <script src="{{ asset('bundles/tariff/external/bootstrap/jquery.min.js') }}"></script>
    <script src="{{ asset('bundles/tariff/external/bootstrap/bootstrap.min.js') }}"></script>
{% endblock %}
</body>
</html>
